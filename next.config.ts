import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // eslint: {
  //   ignoreDuringBuilds: true, // <— THIS disables ESLint from blocking builds
  // },
};

export default nextConfig;
